def resta_matrices(mat1: list[list[int]], mat2: list[list[int]]) -> list[list[int]] | None:
    """
    Resta dos matrices de igual tamaño (mismas filas y mismas columnas). 
    Si las matrices recibidas no tuvieran el mismo tamaño, la función devolvería None.
    
    Parámetros:
    mat1 (list[list[int]]): Primera matriz.
    mat2 (list[list[int]]): Segunda matriz.

    Devuelve:
    list[list[int]] | None: La matriz resultante de restar mat2 a mat1, o None si las matrices no tienen el mismo tamaño.
    """
    # TODO: Borra este comentario y la instrucción pass, e implementa la función
    pass