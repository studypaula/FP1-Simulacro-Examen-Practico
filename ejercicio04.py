from datetime import date, timedelta

def calcula_gastos(gasto_diario: list[float], fecha_inicio: date, fecha_fin:date) -> float:
    """
    Calcula el gasto total entre dos fechas, sumando el gasto de cada día. La función
    recibe una lista con el gasto diario según día de la semana (0=lunes, 1=martes, ..., 6=domingo).

    Parámetros:
    gasto_diario (list[float]): Lista con el gasto diario según día de la semana (0=lunes, 1=martes, ..., 6=domingo).
    fecha_inicio (date): Fecha de inicio del periodo.
    fecha_fin (date): Fecha de fin del periodo.

    Devuelve:
    float: El gasto total entre las dos fechas.
    """
    # TODO: Borra este comentario y la instrucción pass, e implementa la función
    pass